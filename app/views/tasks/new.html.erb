<h1>Create new task</h1>

<%= form_for(@task) do |f| %>
  <div>
    <%= f.text_area :name, placeholder: "Compose new task...Name" %>
    <%= f.text_area :content, placeholder: "content" %>
    <%= date_select :task, :date_from %>
  </div>
  <%= f.submit "Post", class: "btn" %>
<% end %>